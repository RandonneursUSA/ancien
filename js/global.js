/**
 * @file
 * Global utilities.
 *
 */
(function ($, Drupal) {

  'use strict';

  Drupal.behaviors.ancien = {
    attach: function (context, settings) {
 /* Will make menu dropdown on hover
     $(".dropdown").hover(function() {
       $('.dropdown-toggle', this).trigger('click'); 
      });
 */
    }
  };

})(jQuery, Drupal);
